<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Admin Authentication Credentials
    |--------------------------------------------------------------------------
    |
    | These credentials are used for HTTP Basic Authentication to protect
    | the admin interface. Set these in your .env file.
    |
    */

    'username' => env('ADMIN_USERNAME', ''),
    'password' => env('ADMIN_PASSWORD', ''),
];
